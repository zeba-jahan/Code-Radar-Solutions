#include<stdio.h>
int main(){
    int num;
    
    scanf("%d",&num);
    if (num % 5)
    printf("Divisible\n",num);
    else if (num % 11)
    printf("Divisible\n",num);
    else (num !% 5,11)
    printf("Not Divisible\n",num);
    return 0;
}

